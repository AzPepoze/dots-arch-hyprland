# Keep the visual QuickShell lock screen when it is available.  hyprlock is a
# safe fallback, and pidof prevents a second hyprlock instance.
$lock_cmd = hyprctl dispatch 'hl.dsp.global("quickshell:lock")' || pidof hyprlock || hyprlock
$suspend_cmd = systemctl suspend || loginctl suspend
$resume_cmd = $HOME/.config/hypr/scripts/resume.sh

general {
    lock_cmd = $lock_cmd
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = $resume_cmd
    inhibit_sleep = 3
}

listener {
    timeout = 300 # 5mins
    on-timeout = loginctl lock-session
}

listener {
    timeout = 600 # 10mins
    on-timeout = hyprctl dispatch 'hl.dsp.dpms({ action = "disable" })'
    on-resume = hyprctl dispatch 'hl.dsp.dpms({ action = "enable" })'
}

listener {
    timeout = 900 # 15mins
    on-timeout = $suspend_cmd
}
