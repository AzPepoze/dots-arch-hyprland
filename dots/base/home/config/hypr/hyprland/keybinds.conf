#-------------------------------------------------------
# [Variables]
#-------------------------------------------------------
$dots = ~/dots-arch-hyprland
$qsConfig = ii
$workspace = Alt
$TERMINAL = kitty
$EXPLORER = dolphin
$moveactivewindow=grep -q "true" <<< $(hyprctl activewindow -j | jq -r .floating) && hyprctl dispatch moveactive

#-------------------------------------------------------
# [Shell & System Controls]
#-------------------------------------------------------

#!
##! Shell & Widgets
# Quickshell Integration (Added from Base)
# These need to be on top for consistency
# bindid = Super, Super_L, Toggle search, global, quickshell:searchToggleRelease # Toggle search (Quickshell)
# bindid = Super, Super_R, Toggle search, global, quickshell:searchToggleRelease # [hidden]
binditn = Super, catchall, global, quickshell:searchToggleReleaseInterrupt # [hidden]

binddl = Super, Super_L, Toggle launcher, exec, ulauncher-toggle # Toggle overview/launcher
bindd = Super, Tab, Toggle overview, global, quickshell:overviewWorkspacesToggle # Toggle overview
# Fallbacks for overview
bind = Ctrl, Super_L, global, quickshell:overviewToggleReleaseInterrupt # [hidden]
bind = Super, mouse:272, global, quickshell:overviewToggleReleaseInterrupt # [hidden]

# Sidebars & Panels
bind = Super, B, global, quickshell:sidebarLeftToggle # [hidden] Toggle Left Sidebar
bind = Super, O, global, quickshell:sidebarLeftToggle # [hidden]
bind = Super+Alt, A, global, quickshell:sidebarLeftToggleDetach # [hidden]
bindd = Super, N, Toggle right sidebar, global, quickshell:sidebarRightToggle # Toggle right sidebar
bindd = Super, J, Toggle bar, global, quickshell:barToggle # Toggle bar
bindd = Super, Slash, Toggle cheatsheet, global, quickshell:cheatsheetToggle # Toggle cheatsheet
bindd = Super, K, Toggle on-screen keyboard, global, quickshell:oskToggle # Toggle on-screen keyboard
bindd = Super, M, Toggle media controls, global, quickshell:mediaControlsToggle # Toggle media controls
bind = Super, G, global, quickshell:overlayToggle # Toggle overlay
bind = Super+Alt, W, global, quickshell:panelFamilyCycle # Cycle panel family

# Clipboard & Emoji
bindd = Super, V, Clipboard history, global, quickshell:overviewClipboardToggle # Clipboard history >> clipboard
bindd = Super, Semicolon, Emoji picker, global, quickshell:overviewEmojiToggle # Emoji >> clipboard

# Wallpaper & Session
bindd = Ctrl+Super, T, Toggle wallpaper selector, global, quickshell:wallpaperSelectorToggle # Wallpaper selector
bindd = Ctrl+Super+Alt, T, Select random wallpaper, global, quickshell:wallpaperSelectorRandom # Random wallpaper
bind = Ctrl+Super, R, exec, killall ags agsv1 gjs ydotool qs quickshell; qs -c $qsConfig & # Restart widgets
bind = Shift+Super+Alt, Slash, exec, qs -p ~/.config/quickshell/$qsConfig/welcome.qml # [hidden] Launch welcome app
bindd = Ctrl+Alt, Delete, Toggle session menu, global, quickshell:sessionToggle # Toggle session menu

# Legacy / User Fallbacks
bind = ALT_R, Control_R, exec, killall waybar || waybar # Toggle waybar
bind = Ctrl Alt, Delete, exec, $dots/scripts/logoutlaunch.sh # Logout menu (Fallback)

#-------------------------------------------------------
# [Hardware Controls]
#-------------------------------------------------------

# Hardware
# Brightness
bindle=, XF86MonBrightnessUp, exec, qs -c $qsConfig ipc call brightness increment || brightnessctl s 5%+ # [hidden]
bindle=, XF86MonBrightnessDown, exec, qs -c $qsConfig ipc call brightness decrement || brightnessctl s 5%- # [hidden]

# Volume & Mute
bindle=, XF86AudioRaiseVolume, exec, wpctl set-volume -l 1.5 @DEFAULT_AUDIO_SINK@ 2%+ # [hidden]
bindle=, XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%- # [hidden]
bindl = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_SINK@ toggle # [hidden]
bindld = Super+Shift,M, Toggle mute, exec, wpctl set-mute @DEFAULT_SINK@ toggle # [hidden]
bindl = Alt ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_SOURCE@ toggle # [hidden]
bindl = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_SOURCE@ toggle # [hidden]
bindld = Shift+Ctrl,D, Toggle mic, exec, wpctl set-mute @DEFAULT_SOURCE@ toggle # [hidden]
bindld = Super+Alt,M, Toggle mic, exec, wpctl set-mute @DEFAULT_SOURCE@ toggle # [hidden] (Base version)

# Keyboard Layout
bindl = Alt_L, Shift_L, exec, $dots/scripts/keybinds/keyboardswitch.sh
bindl = Shift_L, Alt_L, exec, $dots/scripts/keybinds/keyboardswitch.sh

#-------------------------------------------------------
# [Utilities]
#-------------------------------------------------------

##! Utilities
# Screenshot, Record, OCR, Color picker
bindd = , Print, Screen snip, global, quickshell:regionScreenshot # Screen snip (Quickshell)
bindd = Super+Shift, S, Screen snip, global, quickshell:regionScreenshot # Screen snip (Quickshell)
bindd = Super, Print, Capture screenshot, exec, mkdir -p $(xdg-user-dir PICTURES)/Screenshots && FILE=$(xdg-user-dir PICTURES)/Screenshots/Screenshot_$(date +'%Y-%m-%d_%H-%M-%S').png && grim "$FILE" && wl-copy < "$FILE" && notify-send "Screenshot Saved" "Image saved to $FILE" # Capture screen
# bind = Super+Shift, S, exec, qs -c $qsConfig ipc call TEST_ALIVE || pidof slurp || hyprshot --freeze --clipboard-only --mode region --silent # [hidden] Fallback

bind = Super+Shift, A, global, quickshell:regionSearch # Google Lens (Quickshell)
# [Maple] ระวัง: Super+A ของ User เดิมคือ Rofi (App Launcher) บรรทัดนี้อาจชนกัน
# bind = Super+Shift, A, exec, qs -c $qsConfig ipc call TEST_ALIVE || pidof slurp || ~/.config/hypr/hyprland/scripts/snip_to_search.sh # [hidden] Fallback

# OCR
bind = Super+Shift, T, global, quickshell:regionOcr # Character recognition >> clipboard
# bind = Super+Shift, T, global, quickshell:regionOcr # [hidden]
# Fallback OCR (User defined)
# bindd = Super+Shift, T, Character recognition,exec,grim -g "$(slurp $SLURP_ARGS)" "tmp.png" && tesseract "tmp.png" - | wl-copy && rm "tmp.png" # [hidden]

# Color picker
bindd = Super+Shift, C, Color picker, exec, hyprpicker -a # Pick color (Hex) >> clipboard

# QR code scanner
bindd = Super, Q, QR Code Scanner, exec, $dots/scripts/utils/qr_code_scanner.sh

# Recording
bindl = Super+Shift, R, global, quickshell:regionRecord # Record region (no sound)
bindl = Super+Shift, R, exec, qs -c $qsConfig ipc call TEST_ALIVE || ~/.config/quickshell/$qsConfig/scripts/videos/record.sh # [hidden] Fallback
bindd = Super+Alt, R, Record region (no sound), exec, ~/.config/hypr/hyprland/scripts/record.sh # User Script
bindd = Ctrl+Alt, R, Record screen (no sound), exec, ~/.config/hypr/hyprland/scripts/record.sh --fullscreen # [hidden]
bindd = Super+Shift+Alt, R, Record screen (with sound), exec, ~/.config/hypr/hyprland/scripts/record.sh --fullscreen-sound # User Script

# AI
bindd = Super+Shift+Alt, mouse:273, Generate AI summary for selected text, exec, ~/.config/hypr/hyprland/scripts/ai/primary-buffer-query.sh # AI summary

#-------------------------------------------------------
# [Application Launchers]
#-------------------------------------------------------

#! 
##! Apps
bind = Super, T, exec, $TERMINAL # Terminal emulator
bind = Alt Ctrl, T, exec, [float; move 20% 5%; size 60% 60%] $TERMINAL # Dropdown terminal
bind = Super, E, exec, $EXPLORER # File explorer
bind = Super+Shift, W, exec, ~/.config/hypr/hyprland/scripts/launch_first_available.sh "wps" "onlyoffice-desktopeditors" # Office software
bind = Super, X, exec, ~/.config/hypr/hyprland/scripts/launch_first_available.sh "kate" "gnome-text-editor" "emacs" # Text editor
bind = Super, I, exec, XDG_CURRENT_DESKTOP=gnome ~/.config/hypr/hyprland/scripts/launch_first_available.sh "qs -p ~/.config/quickshell/$qsConfig/settings.qml" "systemsettings" "gnome-control-center" "better-control" # Settings app
bind = Control Shift, Escape, exec, [float] missioncenter # System monitor

# [CHECK] Super+A เดิมใช้เปิด Rofi แต่ Base config ใช้เปิด Sidebar ซ้าย (Quickshell)
# เมเปิ้ลแนะนำให้ใช้ Rofi เหมือนเดิม แต่ถ้าอยากใช้ Sidebar ให้เปลี่ยนปุ่ม
bind = Super, A, exec, pgrep -x rofi && killall rofi || rofi -show drun

# bind = Super, W, exec, ~/.config/hypr/hyprland/scripts/launch_first_available.sh "google-chrome-stable" "zen-browser" "firefox" "brave" "chromium" "microsoft-edge-stable" "opera" "librewolf" # Browser
# bind = Super, C, exec, ~/.config/hypr/hyprland/scripts/launch_first_available.sh "code" "codium" "cursor" "zed" "zedit" "zeditor" "kate" "gnome-text-editor" "emacs" "command -v nvim && kitty -1 nvim" "command -v micro && kitty -1 micro" # Code editor
bind = Ctrl+Super, V, exec, ~/.config/hypr/hyprland/scripts/launch_first_available.sh "pavucontrol-qt" "pavucontrol" # Volume mixer

#-------------------------------------------------------
# [Media & Miscellaneous]
#-------------------------------------------------------

##! Media & Misc
bind = , XF86AudioPlay, exec, playerctl play-pause # Play media
bind = , XF86AudioPause, exec, playerctl play-pause # Pause media
bind = , XF86AudioNext, exec, playerctl next # Next media
bind = , XF86AudioPrev, exec, playerctl previous # Previous media
bindl= Super+Shift, N, exec, playerctl next || playerctl position `bc <<< "100 * $(playerctl metadata mpris:length) / 1000000 / 100"` # Next track
bindl= Super+Shift, B, exec, playerctl previous # Previous track
bindl= Super+Shift, P, exec, playerctl play-pause # Play/pause media

# Zoom
binde = Super, Minus, exec, qs -c $qsConfig ipc call zoom zoomOut # Zoom out
binde = Super, Equal, exec, qs -c $qsConfig ipc call zoom zoomIn # Zoom in
binde = Super, Minus, exec, qs -c $qsConfig ipc call TEST_ALIVE || ~/.config/hypr/hyprland/scripts/zoom.sh decrease 0.1 # [hidden] Fallback
binde = Super, Equal, exec, qs -c $qsConfig ipc call TEST_ALIVE || ~/.config/hypr/hyprland/scripts/zoom.sh increase 0.1 # [hidden] Fallback

# Session
bind = Super+Shift, L, exec, loginctl lock-session # Lock
bindld = Super+Shift, L, Suspend system, exec, sleep 0.1 && systemctl suspend || loginctl suspend # Sleep
bindd = Ctrl+Shift+Alt+Super, Delete, Shutdown, exec, systemctl poweroff || loginctl poweroff # [hidden] Power off
bind = Super, Delete, exit, # Kill hyprland session

# Testing / Fun
bind = Super+Alt, f11, exec, bash -c 'RANDOM_IMAGE=$(find ~/Pictures -type f | grep -v -i "nipple" | grep -v -i "pussy" | shuf -n 1); ACTION=$(notify-send "Test notification with body image" "This notification should contain your user account <b>image</b> and <a href=\"https://discord.com/app\">Discord</a> <b>icon</b>. Oh and here is a random image in your Pictures folder: <img src=\"$RANDOM_IMAGE\" alt=\"Testing image\"/>" -a "Hyprland keybind" -p -h "string:image-path:/var/lib/AccountsService/icons/$USER" -t 6000 -i "discord" -A "openImage=Open profile image" -A "action2=Open the random image" -A "action3=Useless button"); [[ $ACTION == *openImage ]] && xdg-open "/var/lib/AccountsService/icons/$USER"; [[ $ACTION == *action2 ]] && xdg-open "$RANDOM_IMAGE"' # [hidden]
bind = Super+Alt, f12, exec, bash -c 'RANDOM_IMAGE=$(find ~/Pictures -type f | grep -v -i "nipple" | grep -v -i "pussy" | shuf -n 1); ACTION=$(notify-send "Test notification" "This notification should contain a random image in your <b>Pictures</b> folder and <a href=\"https://discord.com/app\">Discord</a> <b>icon</b>.\n<i>Flick right to dismiss!</i>" -a "Discord (fake)" -p -h "string:image-path:$RANDOM_IMAGE" -t 6000 -i "discord" -A "openImage=Open profile image" -A "action2=Useless button" -A "action3=Cry more"); [[ $ACTION == *openImage ]] && xdg-open "/var/lib/AccountsService/icons/$USER"' # [hidden]
bind = Super+Alt, Equal, exec, notify-send "Urgent notification" "Ah hell no" -u critical -a 'Hyprland keybind' # [hidden]

#-------------------------------------------------------
# [Window Management]
#-------------------------------------------------------

#!
##! Window

# Focus Window (User Style: Alt)
bind = $workspace, Left, movefocus, l # Focus left
bind = $workspace, Right, movefocus, r # Focus right
bind = $workspace, Up, movefocus, u # Focus up
bind = $workspace, Down, movefocus, d # Focus down
bind = Super, BracketLeft, movefocus, l # [hidden]
bind = Super, BracketRight, movefocus, r # [hidden]

# Move Window
bindm = $workspace, mouse:272, movewindow # Hold to move window
bindm = Super, mouse:272, movewindow # [hidden] Base style
bindm = $workspace, Z, movewindow # Hold to move window
bind = Super Shift Control, left, exec, $moveactivewindow -30 0 || hyprctl dispatch movewindow l # Move active window to the left
bind = Super Shift Control, right, exec, $moveactivewindow 30 0 || hyprctl dispatch movewindow r # Move active window to the right
bind = Super Shift Control, up, exec, $moveactivewindow 0 -30 || hyprctl dispatch movewindow u # Move active window up
bind = Super Shift Control, down, exec, $moveactivewindow 0 30 || hyprctl dispatch movewindow d # Move active window down

# Resize Window
bindm = $workspace, mouse:273, resizewindow # Hold to resize window
bindm = Super, mouse:273, resizewindow # [hidden] Base style
bindm = $workspace, X, resizewindow # Hold to resize window

# Split & Ratio (From Base)
binde = Super, Semicolon, splitratio, -0.1 # [hidden]
binde = Super, Apostrophe, splitratio, +0.1 # [hidden]
bind = Ctrl+Super, Backslash, resizeactive, exact 640 480 # [hidden] Make window 640x480

# Close / Kill Window
bind = Alt, F4, killactive # Close focused window
bind = $workspace, C, killactive # Close focused window
# bind = Super, Q, killactive # [hidden] Base style close
bind = Super, c, exec, $dots/scripts/dontkillsteam.sh # Close focused window (Steam safe)
bind = Super, F4, forcekillactive # Forcefully zap a window
# bind = Super+Shift+Alt, Q, exec, hyprctl kill # Forcefully zap a window (Cursor)

# Window State
bind = $workspace Shift, Z, exec, hyprctl dispatch togglefloating activewindow && hyprctl dispatch pin activewindow, # Toggle pin on focused window
bind = $workspace Shift Ctrl, Z, exec, hyprctl dispatch togglefloating activewindow # Float/tile
bind = Super+Alt, Space, togglefloating # [hidden] Base float/tile
bindd = $workspace, W, $d Toggle maximized, fullscreen, 1
bind = Super, D, fullscreen, 1 # [hidden] Base Maximize
bind = $workspace, F, fullscreen, 0 # Toggle fullscreen
bind = Super+Alt, F, fullscreenstate, 0 3 # Fullscreen spoof
bind = Super, P, pin # Pin window

# Cycle Windows
bind = ALT, Tab, cyclenext, # Cycle focus
bind = Alt, Tab, bringactivetotop, # [hidden] bring it to the top

# Window Group / Title Bar
bindd = Super, W, Toggle Group (Title Bar), togglegroup
bind = Alt, Tab, changegroupactive, f

#-------------------------------------------------------
# [Workspace Management]
#-------------------------------------------------------

##! Workspace
# Switch Workspace (Direct)
bind = $workspace, 1, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh workspace 1 # [hidden]
bind = $workspace, 2, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh workspace 2 # [hidden]
bind = $workspace, 3, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh workspace 3 # [hidden]
bind = $workspace, 4, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh workspace 4 # [hidden]
bind = $workspace, 5, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh workspace 5 # [hidden]
bind = $workspace, 6, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh workspace 6 # [hidden]
bind = $workspace, 7, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh workspace 7 # [hidden]
bind = $workspace, 8, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh workspace 8 # [hidden]
bind = $workspace, 9, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh workspace 9 # [hidden]
bind = $workspace, 0, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh workspace 10 # [hidden]

# Switch Workspace (Relative)
bind = Super, Right, workspace, r+1 # [hidden]
bind = Super, Left, workspace, r-1 # [hidden]
bind = Super, Page_Down, workspace, +1 # [hidden]
bind = Super, Page_Up, workspace, -1 # [hidden]
bind = Ctrl+Super, Page_Down, workspace, r+1 # [hidden]
bind = Ctrl+Super, Page_Up, workspace, r-1 # [hidden]
bind = Super, mouse_up, workspace, +1 # [hidden]
bind = Super, mouse_down, workspace, -1 # [hidden]
bind = Ctrl+Super, mouse_up, workspace, r+1 # [hidden]
bind = Ctrl+Super, mouse_down, workspace, r-1 # [hidden]
bind = Ctrl+Super, BracketLeft, workspace, -1 # [hidden]
bind = Ctrl+Super, BracketRight, workspace, +1 # [hidden]

# Move Window to Workspace (Direct)
bind = $workspace Shift, 1, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspace 1 # [hidden]
bind = $workspace Shift, 2, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspace 2 # [hidden]
bind = $workspace Shift, 3, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspace 3 # [hidden]
bind = $workspace Shift, 4, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspace 4 # [hidden]
bind = $workspace Shift, 5, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspace 5 # [hidden]
bind = $workspace Shift, 6, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspace 6 # [hidden]
bind = $workspace Shift, 7, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspace 7 # [hidden]
bind = $workspace Shift, 8, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspace 8 # [hidden]
bind = $workspace Shift, 9, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspace 9 # [hidden]
bind = $workspace Shift, 0, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspace 10 # [hidden]

# Move Silent (Ctrl)
bind = $workspace Ctrl, 1, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspacesilent 1 # [hidden]
bind = $workspace Ctrl, 2, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspacesilent 2 # [hidden]
bind = $workspace Ctrl, 3, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspacesilent 3 # [hidden]
bind = $workspace Ctrl, 4, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspacesilent 4 # [hidden]
bind = $workspace Ctrl, 5, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspacesilent 5 # [hidden]
bind = $workspace Ctrl, 6, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspacesilent 6 # [hidden]
bind = $workspace Ctrl, 7, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspacesilent 7 # [hidden]
bind = $workspace Ctrl, 8, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspacesilent 8 # [hidden]
bind = $workspace Ctrl, 9, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspacesilent 9 # [hidden]
bind = $workspace Ctrl, 0, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspacesilent 10 # [hidden]

# Move Window to Workspace (Relative)
bind = Super+Shift, mouse_down, movetoworkspace, r-1 # [hidden]
bind = Super+Shift, mouse_up, movetoworkspace, r+1 # [hidden]
bind = Super+Alt, mouse_down, movetoworkspace, -1 # [hidden]
bind = Super+Alt, mouse_up, movetoworkspace, +1 # [hidden]
bind = Super+Alt, Page_Down, movetoworkspace, +1 # [hidden]
bind = Super+Alt, Page_Up, movetoworkspace, -1 # [hidden]
bind = Super Control+Alt, Right, movetoworkspace, r+1 # [hidden]
bind = Super Control+Alt, Left, movetoworkspace, r-1 # [hidden]

# Special Workspace (Scratchpad)
bind = $workspace, S, togglespecialworkspace, # Toggle scratchpad
bind = $workspace Shift, S, movetoworkspace, special # Send to scratchpad
bind = Ctrl Shift, S, movetoworkspacesilent, special # Send to scratchpad silent
bind = Super, S, togglespecialworkspace, # [hidden] (Base style)
bind = Super+Alt, S, movetoworkspacesilent, special # [hidden] Send to scratchpad (Base style)